/** Colors consumed by `@minibot/ui` primitives. */
export type UiTheme = {
  text: string;
  textSecondary: string;
  heading: string;
  background: string;
  card: string;
  border: string;
  primary: string;
  onPrimary: string;
  muted: string;
  red: string;
  focus: string;
  green: string;
  yellow: string;
  /** Secondary button / chip fill */
  surface: string;
};
