# GitHub Packages（私有 @liuyidi/*）
# 复制为 .env 后填写（.env 已在 .gitignore，勿提交真实 token）：
#   cp .env.example .env
#
# 创建 PAT：https://github.com/settings/tokens
#   classic: 勾选 read:packages（若还要 push 包再勾 write:packages）
#   或 fine-grained: Packages → Read
#
# 安装依赖：
#   npm run install:deps

NODE_AUTH_TOKEN=
