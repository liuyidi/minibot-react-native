# Peer deps come from the app root — never nest a second React / RN native module here.
omit=peer
# Prefer hoisting; avoid installing duplicates of app-provided peers when possible.
install-strategy=hoisted
